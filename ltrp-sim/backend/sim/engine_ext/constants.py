# -*- coding: utf-8 -*-
"""引擎常量(配置层,Rule 2.1 统一配置、零硬编码)。

所有引擎级可调常量集中于此,各职责模块 import 引用,避免主引擎与子模块的循环依赖。
"""
import random

# ---- 引擎 tick / 定时 ----
DT = 0.3                # tick 周期(秒,实时)
BEACON_EVERY = 1        # 每 tick 都发信标(0.3s,加速初始收敛)
RELAX_ROUNDS = 3        # 每 tick 的 DSDV 松弛轮数
SLEEP_PERIOD = 16.0     # 轮值休眠窗口(长窗口降低拓扑抖动)

# ---- 流量自适应休眠(duty cycling):苏醒比例随负载升降 ----
SLEEP_DUTY_MIN = 0.25   # 最低苏醒比例(低负载:保住连通主干即可,其余省电)
SLEEP_DUTY_MAX = 0.80   # 最高苏醒比例(高负载:多节点转发换取吞吐,并均摊能耗)
LOAD_TARGET = 3.0       # 每存活节点的目标积压(bundles+packets)个
LOAD_HYST = 1.2         # 目标附近死区(避免频繁升降档)
DUTY_STEP = 0.05        # 每次调节苏醒比例的步长

# ---- 月球车摆渡(存储-携带-转发) ----
FERRY_CAP = 128         # 月球车束容量(个):摆渡时按容量收货,不超载
FERRY_GIVE = 4          # 每次卸载转给骨干邻居的束数
DUPE_SEEN_MAX = 4096    # 交付去重表上限(组 id);超限按最旧淘汰,保证确定性

# ---- 月球车物理(实体碰撞) ----
ROVER_R = 14.0          # 物理半径(与巨石/岩壁判定用)
ROVER_WALL_M = 12.0     # 靠壁安全间距
ROVER_PREF = 34.0       # 相对中心线的巡逻偏好偏移
PATROL_LOOK = 60.0      # 巡逻路径前瞻距离
PATROL_XSTEP = 20.0     # 巡逻走廊采样列间距(px)
CARRIER_INSET = 2.0     # 探针纵向目标相对自由带边缘的安全内缩(px):
                        # 贴着巨石边界时目标若正好落在带边缘,会被判为撞石而原地卡死
CARRIER_MAX_SLOPE = 1.0  # 走廊相邻采样点允许的最大纵向坡度(dy/dx):
                         # 限速探针(10cm/s)跟不上更陡的突变,会被巨石夹住
CARRIER_STALL_N = 25    # 卡死看门狗:前进停滞连续 tick 数超过此值 → 倒车脱困
CARRIER_RETREAT = 120.0  # 看门狗触发后的倒退距离(px):退出巨石/岩壁夹出的闭合凹袋

# ---- 塌方落石(明显大于天然巨石 34~56,但硬性保证不封死通路) ----
COLLAPSE_R_MIN = 84.0     # 塌方巨石半径下限(px)
COLLAPSE_R_MAX = 110.0    # 上限(px)
COLLAPSE_PASS_W = 48.0    # 塌方后必须保留的最小可通行带宽(px):
                           # 月球车直径 28 + 靠壁安全距 24 + 余量,放不下就缩石/挪位

# ---- 激光无线充电 (源自 Sensors 2021,21,6203: 母-子架构·多跳 bucket-brigade 功率传输) ----
# 依据 Figure 17 曲线反推的链路效率体系; 为提高作用于"多跳递送到管尾"的功率充足性,
# 将供电提到 kW 级(地表可控核裂变)、压低衰减、提高低损耗反射, 并让每跳富余"部分留存本地电池、
# 其余转发下游"(LASER_STORE), 从而: ① 能量能递送到最深处; ② 不再被中途休眠节点切断(能量树含睡眠节点)。
LASER_PS = 2000.0         # 地表可控核裂变供电功率 W(供电给激光器, kW 级以补偿多跳衰减; 足够递送到管尾)
LASER_ETA_EL = 0.50       # 电→激光效率 ηE-L: Pl(0) = ηE-L * Ps
LASER_ALPHA = 0.0006      # 激光传输衰减系数 α (/m): ηT = e^{-α·d}
LASER_ETA_LE = 0.55       # 激光→电(光伏)效率 ηL-E: Pe(n) = ηL-E * Pi(n)
LASER_ETA_R = 0.95        # 每跳反射效率 ηR: Pl(n) = ηR*(Pe(n) - Psensor/ηL-E)
LASER_STORE = 0.05        # 每跳富余中"留存本地电池"的比例(小: 把尽可能多的功率转发下游, 突破多跳衰减递送到管尾)
BATTERY_V = 3.7           # 电池标称电压 V(用于 W ↔ mA 换算)
BASE_STORAGE_MAH = 1.0e6  # BASE-00 巨型储能电池容量 mAh(接收地表核裂变→激光→电)
ENERGY_EVERY = 1          # 能量/充电调度每 N tick 执行一次

# ---- 移动速度 & 时间缩放 ----
MOVE_V = 0.1             # 探针(面包屑载体)合速度 m/s —— 要求:横向纵向合成不超过 10cm/s
NODE_MOVE_V = 0.26       # 自愈节点移动速度 m/s(26cm/s):对齐 v0 的 26px/tick 观感;
                         # 过慢会让断口两端长时间合不拢 —— 这是"自愈变弱"的直接原因
ROVER_V = 0.55           # 月球车巡逻速度 m/s(55cm/s):它是自愈的"信使"(把断裂对端位置
                         # 摆渡给失联节点),过慢会让失联侧长期拿不到对端坐标
TIME_SCALE_DEFAULT = 100  # 默认时间缩放: 物理 10cm/s × 100 → 演示约 10 m/s, 部署节奏较合理
HEAT_DURATION_S = 22.0   # 温度浪潮持续时长(真实秒): 需按 time_scale 折算为仿真秒,
                         # 否则默认 100x 下一 tick 就跨过整个热浪窗口,按钮形同无效

# ---- 节点移动(自愈重连) ----
NODE_STOP = 5.0            # 到达目标即停止的距离
ISOLATION_T = 3.0          # 判定"孤立求救"的持续无邻居时间
LINK_SAFE = 0.82           # 桥接后与两端距离 ≤ RANGE*LINK_SAFE(留余量,防乒乓)
WOUND_WINDOW = 120.0      # 判定"近期断裂(伤口)"的时间窗(仿真秒,仅此窗口内触发桥接移动)。
                           # 断口两侧(含基站侧)都在此窗口内朝断口靠拢合并;过短(40s 在
                           # 100 倍速下仅 1.3 tick)基站侧刚起步窗口就关,过长(360s 实测)
                           # 基站侧节点会为追断口扯断自己的好链路,级联断链失稳
RELAY_HOLD = 60.0          # rover 注入的"朝网络方向"目标的有效时长
PARTITION_T = 8.0          # 判定"分区失联(持续无基站路由)"的时长;到点后仅"前沿"节点朝锚点桥接(去中心化,不齐动)
PARTITION_SETTLE = 120.0   # pocket 判定阈值(仿真秒):"整片无路由"持续这么久才启动自愈 ——
                           # 给新撒布节点留出 DSDV 收敛时间,避免刚上线就回撤拆链

# ---- 首次部署 / 面包屑(真部署:探针携带道钉,按间距沿途撒布成主干链) ----
DEPLOY_GAP = 130.0         # 面包屑间距: 载体每前进≥此距离就撒下一颗道钉(< 通信半径 RANGE, 保连通)
CARRIER_OFF = 55.0         # 两枚探针相对管道中心线的上下分带偏移:PROBE-1 走 +OFF 上带、
                           # PROBE-2 走 -OFF 下带,在腔室里撒成上/下两股互为冗余的链
DEPLOY_TIMEOUT_MIN = 240.0  # 部署超时兜底下限(模拟秒):超过即就地收尾,防探针被巨石卡死

# ---- 道钉落点防放置(与巨石/岩壁/既有节点保距,与链上上一颗保视距) ----
SPIKE_WALL_M = 12.0        # 落点与岩壁的最小余量(px):贴壁落下会被洞壁弧度挡住链路
SPIKE_BOULDER_M = 14.0     # 落点与巨石表面的最小余量(px):贴石落下等于亲手挡断自己的 LOS
SPIKE_MIN_SEP = 40.0       # 新落点与已放置道钉的最小间距(px):两股链不堆叠、不浪费节点
SPIKE_HOP_MIN = 30.0       # 盲区短跳的最小跳距(px):再近就是堆叠,链没长反而费节点
SPIKE_HOP_MAX = 80.0       # 盲区短跳的最大跳距(px):窄喉道急弯内短距 LOS 几乎必然成立,
                           # 链以此间距逐跳爬过"基站都被挡"的盲区后恢复正常撒布
